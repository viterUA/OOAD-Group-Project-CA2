/**
 * SRP + Abstraction: Defines valid priority levels in a type-safe way.
 * Encapsulation: Centralizes allowed values instead of using raw strings or numbers.
 */
public enum Priority {
    LOW,
    MEDIUM,
    HIGH,
    URGENT
}
